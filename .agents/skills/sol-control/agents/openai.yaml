interface:
  display_name: "Sol Control"
  short_description: "Sol controls evidence-gated execution with bounded repair."
  default_prompt: "使用 $sol-control 规划、分配、执行、验证，并在原授权范围内持续修复或重规划这项任务。"
policy:
  allow_implicit_invocation: false
