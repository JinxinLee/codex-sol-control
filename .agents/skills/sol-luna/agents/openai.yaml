interface:
  display_name: "Sol Luna"
  short_description: "Sol-controlled execution using Luna Max only."
  default_prompt: "使用 $sol-luna，由 Sol 负责规划和审核，所有委派执行任务只使用 Luna Max。"
policy:
  allow_implicit_invocation: false
